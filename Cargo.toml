[package]
name = "spadebox"
version = "0.1.0"
edition = "2024"

[lib]
name = "spadebox"
path = "src/lib.rs"

[dependencies]
cap-std = "3"
thiserror = "2"
tokio = { version = "1", features = ["fs", "io-util", "rt-multi-thread", "macros"] }
