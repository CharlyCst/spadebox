name: spadebox_dart_example
description: SpadeBox agent — pure Dart CLI example.
version: 1.0.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter_rust_bridge: ^2.9.0
  spadebox_dart:
    path: ../../dart
